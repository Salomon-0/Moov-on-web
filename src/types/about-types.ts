export type Value = {
    number: string;
    title: string;
    description: string;
    emoji: string;
  };
  
  export type TeamMember = {
    name: string;
    role: string;
    initials: string;
    color: string;
  };
  
  export type Stat = {
    value: string;
    label: string;
    suffix: string;
  };
  